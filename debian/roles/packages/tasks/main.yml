---
- name: Install packages
  apt: "{{ item }}"
  with_items:
    - "{{ themes }}" 
    - "{{ desktop_utilities }}" 
    - "{{ accessories }}" 
    - "{{ clients }}" 
    - "{{ games }}" 
    - "{{ bash }}" 
    - "{{ sys }}" 
