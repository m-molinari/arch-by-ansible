---
- hosts: localhost
  connection: local
  become: yes
  become_user: root
  roles:
   - iptables
   - { role: "yay", when: (ansible_distribution == 'Archlinux') }
   - { role: "rsyslog", when: (ansible_distribution == 'Archlinux') }
   - { role: "services", when: (ansible_distribution == 'Archlinux') }
